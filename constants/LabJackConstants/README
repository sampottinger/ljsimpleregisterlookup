LabJack constants

=====================
  Windows Install
=====================

To install, copy or move the LabJack folder to %SystemDrive%\ProgramData.

The finished installation is correct when the the ljm_constants.json file can be accessed
as:
    %SystemDrive%\ProgramData\LabJack\LJM\ljm_constants.json


=====================
  Mac/Linux Install
=====================

To install, run the install.sh script contained in the same directory as this
README file. For example:

	$ cd path/to/LabJackConstants/
	$ sudo ./install.sh
